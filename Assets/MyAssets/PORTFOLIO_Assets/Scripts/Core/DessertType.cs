namespace Assets.MyAssets.PORTFOLIO_Assets.Scripts.Core
{
    /// <summary>
    /// 디저트 5종. 순서를 바꾸지 말 것.
    /// (int)DessertType 을 DessertTable 의 배열 인덱스로 쓰기 때문에,
    /// 순서가 바뀌면 인스펙터에 꽂아 둔 스프라이트가 전부 어긋난다.
    /// </summary>
    public enum DessertType
    {
        ChocoCake,
        KiwiBigCake,
        LemonRectangularCake,
        RainbowCupCake,
        SkullCakePiece
    }
}
