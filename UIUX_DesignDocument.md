# UI/UX 설계 문서 — 디저트 가게 주문 받기

- 교과목: 게임 UI/UX 프로그래밍
- NCS 능력단위: `0803020529_24v5 게임 UI/UX 프로그래밍`
- 능력단위 요소: 게임 UI/UX 설계하기
- 개발 환경: Unity 6, C#, UGUI, TextMeshPro, Input System
- 작성일: 2026-08-18
- 문서 상태: 초안 (7장 구현 가능성 검토는 구현·테스트 이후 갱신)

---

## 1. 게임 개요

### 1.1 한 줄 설명

제한된 영업시간 안에 손님의 주문을 정확히 처리해 점수를 얻는 캐주얼 UI 게임입니다.

### 1.2 플레이 루프

```text
손님 1명 등장
  └ 주문 카드 표시 (품목 아이콘 + 수량)
      └ 플레이어가 진열대에서 해당 디저트를 눌러 쟁반에 담음
          └ 제출
              ├ 주문과 일치     -> 점수 획득, 처리 건수 +1
              └ 불일치 / 시간초과 -> 실패 건수 +1
                  └ 다음 손님 등장
영업시간 60초 종료 -> 결과 화면
```

### 1.3 설계 판단: 왜 "재료 조합"이 아니라 "메뉴 담기"인가

초기 구상은 `밀가루 + 크림 → 케이크` 형태의 재료 조합이었습니다. 다음 두 가지 이유로 완성 품목을 그대로 선택하는 방식으로 변경했습니다.

| 항목 | 재료 조합 방식 | 채택: 메뉴 담기 방식 |
| :--- | :--- | :--- |
| 필요 리소스 | 원재료 + 완성품 2세트 | 완성품 1세트 |
| 플레이어 인지 부담 | 레시피를 학습·기억해야 함 | 주문 카드만 보면 됨 |
| 추가 UI | 레시피 확인 화면 필요 | 불필요 |
| 정보 매칭 | 주문(완성품)과 조작(재료)이 다른 이미지 | **주문과 조작이 동일한 이미지** |

특히 마지막 항목이 결정적이었습니다. 주문 카드의 아이콘과 진열대 버튼의 아이콘이 같은 스프라이트이므로, 플레이어는 텍스트를 읽지 않고 그림 대조만으로 조작할 수 있습니다.

---

## 2. UI/UX 요소 추출

NCS 기준의 네 가지 인터페이스 구분에 따라 정리합니다.

| 구분 | 이 게임에서의 내용 |
| :--- | :--- |
| 입력 인터페이스 | 마우스 클릭, 터치, 키보드 (Navigate / Submit / Cancel) |
| 게임 화면 인터페이스 | 남은 영업시간 게이지, 점수, 현재 주문 안내, 처리·실패 카운터 |
| 컨트롤 인터페이스 | 진열대 디저트 버튼, 쟁반 취소, 제출 버튼, 일시정지 버튼, 메뉴 버튼 |
| 커스텀 인터페이스 | 손님 주문 카드(말풍선형), 쟁반 슬롯 영역 |

### 2.1 플레이어가 가장 자주 확인하는 정보

1. **현재 주문** — 매 손님마다 바뀌므로 확인 빈도가 가장 높습니다.
2. **남은 영업시간** — 게임 전체의 압박 요소입니다.
3. **쟁반 상태** — 지금까지 무엇을 담았는지.
4. 점수, 처리·실패 건수 — 결과 확인용이며 상시 주시 대상은 아닙니다.

이 우선순위가 4장의 화면 배치 근거가 됩니다.

---

## 3. 화면 목록과 전환 흐름

### 3.1 화면 목록

```text
1. Screen_Title
   - 게임 제목
   - 시작 버튼
   - 종료 버튼

2. Screen_Play
   - 남은 영업시간 게이지 + 숫자
   - 점수 Text
   - 처리 / 실패 카운터
   - 주문 카드 (품목 아이콘 + 수량)
   - 쟁반 슬롯 영역
   - 제출 버튼
   - 진열대 버튼 영역
   - 일시정지 버튼

3. Screen_Pause
   - 계속하기 버튼
   - 타이틀로 돌아가기 버튼 (확인 팝업 경유)

4. Screen_Result
   - 최종 점수
   - 처리 건수 / 실패 건수
   - 다시하기 버튼
   - 타이틀로 버튼
```

### 3.2 전환 흐름

```text
Screen_Title
   │ [시작 버튼] / [Submit]
   ▼
Screen_Play  ──[일시정지 버튼] / [Cancel]──▶ Screen_Pause
   │                                            │
   │                          [계속하기] / [Cancel]
   │                                            │
   │  ◀─────────────────────────────────────────┘
   │
   │ [영업시간 0초 도달]
   ▼
Screen_Result
   │ [다시하기] ──▶ Screen_Play (상태 초기화)
   │ [타이틀로]  ──▶ Screen_Title
   ▼

Screen_Pause ──[타이틀로]──▶ ConfirmPopup ──[예]──▶ Screen_Title
                                          └─[아니오]──▶ Screen_Pause
```

### 3.3 씬 구성 방침

**단일 씬**으로 구성하고 화면 전환은 패널의 활성/비활성으로 처리합니다.

- 씬 로딩 지연이 없어 전환 반응이 즉시 이루어집니다.
- 게임 상태(`GameState`)를 씬 간에 넘길 필요가 없습니다.
- 과제 요건인 "구현 씬 1개 이상"을 충족하며, 채점 시 한 씬에서 전체 흐름을 확인할 수 있습니다.

---

## 4. 화면 배치와 표시 정보

### 4.1 Screen_Play 영역 구분

세로 화면을 네 개 영역으로 나눕니다.

| 영역 | 앵커 | 내용 | 배치 근거 |
| :--- | :--- | :--- | :--- |
| 상단 HUD | Top | 점수, 처리·실패, 시간 게이지, 일시정지 | 상시 정보이며 조작 대상이 아니므로 손가락에 가려지지 않는 상단 |
| 중상단 | Center | 주문 카드 | 확인 빈도 1순위이므로 시선이 먼저 닿는 위치 |
| 중하단 | Center | 쟁반 슬롯, 제출 버튼 | 주문과 진열대 사이에 두어 시선 이동 경로를 단축 |
| 하단 | Bottom | 진열대 버튼 | 조작 빈도가 가장 높으므로 엄지 도달 범위 |

상단과 하단은 각각 Top·Bottom 앵커로 고정하고 중앙 영역만 신축시킵니다. 세로 비율이 다른 기기에서 HUD와 진열대의 위치가 유지됩니다.

### 4.2 표시 정보와 데이터 연결

| 표시 요소 | 연결 데이터 | 갱신 시점 |
| :--- | :--- | :--- |
| 시간 게이지 | `GameState.remainingTime` | 매 프레임 |
| 시간 숫자 | `GameState.remainingTime` (정수 변환) | 값이 바뀔 때만 |
| 점수 | `GameState.score` | 주문 판정 시 |
| 처리 / 실패 | `GameState.successCount`, `failCount` | 주문 판정 시 |
| 주문 카드 | `GameState.currentOrder` (`List<int>`) | 손님 교체 시 |
| 쟁반 슬롯 | `GameState.tray` (`List<int>`) | 담기 / 취소 시 |
| 제출 버튼 활성 | `tray.Count > 0` | 쟁반 변경 시 |

**UI는 데이터를 표시할 뿐 데이터를 소유하지 않습니다.** 점수 변수는 `GameState`에 있고, `HudController`는 그 값을 읽어 TextMeshPro에 반영합니다.

---

## 5. 입력 인터페이스

### 5.1 지원 입력

```text
디저트 담기 : 마우스 좌클릭 / 터치 / Submit(선택 상태에서)
쟁반 취소   : 마우스 좌클릭 / 터치 / Submit
제출        : 마우스 좌클릭 / 터치 / Submit
메뉴 이동   : 방향키 / Tab (Navigate)
확인        : Enter / Space (Submit)
취소·일시정지 : Esc (Cancel)
```

### 5.2 입력별 처리 방침

| 입력 | 구현 방식 | 비고 |
| :--- | :--- | :--- |
| 마우스 | UGUI Button `onClick` | 기본 동작 |
| 터치 | UGUI Button `onClick` | **마우스와 동일 경로**이므로 추가 구현 없음 |
| 키보드 | Input System UI Input Module | Navigate / Submit / Cancel 액션 사용 |

터치와 마우스는 EventSystem에서 동일한 포인터 이벤트로 처리되므로 별도 분기가 필요하지 않습니다.

### 5.3 키보드 대응 설계

- 각 화면을 활성화할 때 `EventSystem.current.SetSelectedGameObject()`로 **첫 버튼을 명시 지정**합니다. 지정하지 않으면 화면 전환 후 키보드 포커스가 사라집니다.
- 진열대 버튼은 가로 1열이므로 Navigation을 `Horizontal` 또는 `Explicit`로 지정합니다. Automatic은 밀집 배치에서 이동 순서가 의도와 어긋납니다.
- `Cancel` 입력은 상황에 따라 동작이 다릅니다.
  - 영업중 → 일시정지 열기
  - 일시정지 → 닫기
  - 확인 팝업 → 팝업만 닫기

### 5.4 범위에서 제외한 항목

| 제외 항목 | 사유 |
| :--- | :--- |
| 게임패드 | 요건은 "키보드 또는 게임패드"이며 키보드로 충족. 검증 기기 확보가 어려움 |
| Safe Area(노치) 대응 | 실기기 배포가 제출 범위가 아님. 상단 HUD에 여백 60px 확보로 갈음 |
| 드래그 앤 드롭 | 포인터 전용 조작이 되어 키보드 대응과 충돌 |

---

## 6. UI 리소스 규칙

### 6.1 에셋 출처와 역할 분리

두 개의 에셋 팩을 사용하되 역할을 명확히 분리해 스타일 혼선을 방지합니다.

| 역할 | 출처 |
| :--- | :--- |
| 디저트 품목 아이콘 | Sweet Cakes Icon Pack (ricimi) |
| 버튼·패널·게이지 배경, 프레임 | Free Casual GUI (Sky Den Games) |
| 시스템 아이콘 (일시정지, 닫기, 체크, 화살표) | Free Casual GUI |
| 글꼴 | Noto Sans KR (SIL Open Font License) |

**품목 아이콘은 Sweet Cakes에서만, UI 부품은 Free Casual GUI에서만 가져옵니다.** 두 팩 모두 아이콘을 포함하지만 혼용하면 선 굵기와 채도가 어긋나므로 위 경계를 지킵니다.

### 6.2 글꼴

| 항목 | 규칙 |
| :--- | :--- |
| 글꼴 | Noto Sans KR 1종 |
| Weight | Regular, Bold 2종만 사용 |
| 굽기 방식 | TMP Font Asset Creator, Custom Characters (사용 문자만) |
| 사용처 | Bold = 점수·시간 등 수치 및 버튼 라벨 / Regular = 안내 문구 |

한글 전체(11,172자)를 굽지 않고 실제 사용 문자만 지정합니다. 아틀라스 크기와 빌드 용량을 억제하기 위함입니다.

### 6.3 색상

| 역할 | 용도 | 비고 |
| :--- | :--- | :--- |
| Primary | 시작·제출 등 주요 행동 버튼 | Free Casual GUI 버튼 색에서 추출 |
| Success | 주문 성공 토스트, 정답 표시 | |
| Danger | 시간 10초 이하 게이지, 오답 표시 | |
| Disabled | 비활성 버튼 | 채도를 낮춘 회색 |
| Text | 본문·수치 | 배경 대비 4.5:1 이상 |
| Text-Sub | 보조 라벨 | 배경 대비 4.5:1 이상 |

색상 값은 Free Casual GUI 스프라이트에서 추출해 배경과 텍스트가 분리되어 보이지 않도록 합니다.

### 6.4 버튼 상태

| 상태 | 표현 |
| :--- | :--- |
| Normal | 기본 색상 |
| Highlighted | 밝기 상승 (마우스 hover / 키보드 선택) |
| Pressed | 축소 + 밝기 하강 |
| Disabled | Disabled 색상 + 라벨 흐림 |

동일한 상태 규칙을 모든 버튼에 적용합니다.

### 6.5 아이콘

| 항목 | 규칙 |
| :--- | :--- |
| 품목 종류 | **4~6종으로 한정** |
| 표시 크기 | 진열대 버튼 내 120px 이상 |
| 사용처 | 주문 카드 / 쟁반 슬롯 / 진열대 버튼 (동일 스프라이트 재사용) |

품목 수를 늘리면 난이도만 상승하고 UI 설계상의 이점이 없으므로 상한을 둡니다.

### 6.6 패널

| 항목 | 규칙 |
| :--- | :--- |
| 배경 | Free Casual GUI 패널 스프라이트, 9-Slice 적용 |
| 모달 배경 | 반투명 검정 오버레이 (뒤 화면 조작 차단) |
| 여백 | 내부 콘텐츠와 테두리 사이 최소 24px |

### 6.7 효과음

이번 과제 범위에서 **제외**합니다. 요건에 포함되지 않으며, 다른 항목 완료 후 여유가 있을 경우에만 성공·실패 2종을 추가합니다.

---

## 7. GUI 디자인 가이드와 구현 가능성 검토

### 7.1 기준 해상도와 Canvas Scaler

| 항목 | 값 |
| :--- | :--- |
| 화면 방향 | 세로 (Portrait) |
| 기준 해상도 | **1080 × 1920** |
| UI Scale Mode | Scale With Screen Size |
| Match Width Or Height | **0 (Width 기준)** |
| Reference Pixels Per Unit | 100 |

**Match를 0으로 둔 이유:** 모바일 기기는 세로 비율 편차가 크지만(16:9 ~ 20:9) 가로는 상대적으로 일정합니다. 폭 기준으로 스케일하면 UI 폭이 항상 화면에 맞고 세로 방향으로만 여유가 생깁니다. Height 기준(Match 1)으로 두면 좁은 기기에서 버튼이 화면 밖으로 벗어납니다.

### 7.2 최소 터치 영역

기준 해상도 폭 1080px을 3배 밀도로 환산하면 360dp이므로 **1dp = 3px**입니다.

| 기준 | dp / pt | 기준 해상도 px |
| :--- | :--- | :--- |
| Android 최소 터치 타겟 | 48dp | 144px |
| iOS 최소 터치 타겟 | 44pt | 132px |
| **본 프로젝트 채택값** | — | **144px 이상** |
| 요소 간 최소 간격 | 8dp | 24px |

진열대 버튼은 아이콘을 포함하므로 200~240px로 배치해 기준을 충분히 상회합니다.

### 7.3 글자 대비

| 대상 | 기준 |
| :--- | :--- |
| 본문·수치 대 배경 | 4.5:1 이상 |
| 버튼 라벨 대 버튼 배경 | 4.5:1 이상 |
| 비활성 라벨 | 기준 미적용 (비활성임을 나타내는 표현) |

캐주얼 UI 에셋은 채도가 높아 흰색 라벨의 대비가 부족한 경우가 있습니다. 미달 시 라벨 색을 어둡게 조정하거나 외곽선을 추가합니다.

### 7.4 정보 우선순위

| 순위 | 정보 | 표현 강도 |
| :--- | :--- | :--- |
| 1 | 현재 주문 | 화면 중앙, 카드 배경으로 강조, 아이콘 대형 |
| 2 | 남은 영업시간 | 게이지 + 숫자 병기, 10초 이하 시 색 변화 |
| 3 | 쟁반 상태 | 담김/빈칸을 실선/점선으로 구분 |
| 4 | 점수 | 상단 좌측, 수치만 |
| 5 | 처리·실패 | 상단 우측, 소형 |

### 7.5 지원 입력 방식 정리

| 입력 | 지원 | 검증 방법 |
| :--- | :--- | :--- |
| 마우스 클릭 | 지원 | 에디터 Game 뷰 |
| 터치 | 지원 | Simulator 또는 Game 뷰 (동일 이벤트 경로) |
| 키보드 Navigate | 지원 | 방향키·Tab으로 전 화면 순회 |
| 키보드 Submit | 지원 | Enter |
| 키보드 Cancel | 지원 | Esc |
| 게임패드 | 미지원 | 범위 제외 |

### 7.6 테스트 대상 해상도

| 해상도 | 비율 | 대표 기기 |
| :--- | :--- | :--- |
| 1080 × 1920 | 16:9 | 기준 해상도 |
| 1080 × 2340 | 19.5:9 | 최근 스마트폰 |
| 1080 × 2400 | 20:9 | 긴 화면 기기 |
| 1536 × 2048 | 3:4 | 태블릿 |

가장 짧은 비율인 태블릿(3:4)에서 세로 공간 부족 문제가 먼저 드러날 것으로 예상됩니다.

### 7.7 구현 가능성 검토 결과

> **작성 안내:** 본 절은 실제 구현·테스트 이후에 채웁니다. 아래는 설계 단계에서 예상한 위험 요소이며, 검증 후 실제 결과로 대체하거나 확정합니다.

| No | 예상 위험 | 원인 | 예상 대응 | 검증 결과 |
| :-- | :--- | :--- | :--- | :--- |
| 1 | 버튼 배경 모서리 깨짐 | 9-Slice Border 미설정 상태에서 폭 신축 | Sprite Editor에서 Border 지정, Image Type을 Sliced로 변경 | *(구현 후 기록)* |
| 2 | 마우스 클릭 후 선택 테두리 잔존 | 포인터 입력 후에도 EventSystem selected 유지 | 포인터 입력 감지 시 `SetSelectedGameObject(null)` | *(구현 후 기록)* |
| 3 | 태블릿 비율에서 레이아웃 붕괴 | 중앙 영역 세로 공간 부족 | 주문 카드·쟁반 크기를 비율 기반으로 조정 | *(구현 후 기록)* |
| 4 | 긴 한글 문구에서 버튼 라벨 넘침 | 고정 폭 버튼에 가변 길이 텍스트 | TMP Auto Size 또는 문구 축약 | *(구현 후 기록)* |
| 5 | 화면 전환 후 키보드 포커스 소실 | 활성 버튼 미지정 | 화면 활성화 시 첫 버튼 명시 선택 | *(구현 후 기록)* |

---

## 8. UI Prefab 후보

| Prefab | 구성 | 재사용 지점 |
| :--- | :--- | :--- |
| **DessertIcon** | Image + 수량 Text | 주문 카드 / 쟁반 슬롯 / 진열대 버튼 — **3개 컨텍스트** |
| **MenuButton** | Button + Label + 상태 색상 | 타이틀 / 일시정지 / 결과 — **4개 화면** |
| GaugeBar | 배경 Image + Filled Image + Label | 영업시간 표시 |
| ToastMessage | 패널 + Text + 페이드 | 성공 / 오답 알림 |
| ConfirmPopup | 모달 배경 + 문구 + 확인/취소 | 타이틀 복귀 확인 |

과제 요건은 2개 이상이며, 상위 두 개(`DessertIcon`, `MenuButton`)만으로도 충족합니다. 나머지 세 개는 화면 구성상 필요하여 함께 제작합니다.

`DessertIcon`은 동일 프리팹이 세 곳에서 서로 다른 크기·역할로 사용되므로 재사용성을 가장 명확히 보여주는 사례입니다.

---

## 9. UI/UX 아키텍처

### 9.1 계층 구조

```text
Game State       -> GameState (score, remainingTime, currentOrder, tray, counts)
UI Controller    -> HudController, OrderCardView, TrayView, ShelfView,
                    ScreenManager, ToastController
UGUI View        -> TextMeshProUGUI, Image, Button
Input System     -> Navigate, Submit, Cancel, Pointer
Screen Flow      -> Title, Play, Pause, Result (단일 씬 패널 전환)
```

데이터는 "실제 점수"이고 UI는 "점수판"입니다. `HudController`가 표시하는 숫자를 바꿔도 `GameState.score`는 변하지 않으며, 반대로 `GameState`가 바뀌면 UI가 이를 반영하도록 연결합니다.

### 9.2 Unity 오브젝트 구조 초안

```text
Canvas (Screen Space - Overlay, 1080x1920, Match 0)
└── UIRoot
    ├── Screen_Title
    │   ├── TitleLabel
    │   ├── MenuButton_Start
    │   └── MenuButton_Quit
    │
    ├── Screen_Play
    │   ├── HudTop
    │   │   ├── ScoreText
    │   │   ├── CountText
    │   │   ├── GaugeBar_Time
    │   │   └── Button_Pause
    │   ├── OrderCard
    │   │   ├── CustomerLabel
    │   │   └── OrderItemList   (DessertIcon 동적 생성)
    │   ├── TraySlots           (DessertIcon 동적 생성)
    │   ├── Button_Submit
    │   └── Shelf               (DessertIcon 포함 버튼 배치)
    │
    ├── Screen_Pause
    │   ├── MenuButton_Resume
    │   └── MenuButton_ToTitle
    │
    ├── Screen_Result
    │   ├── FinalScoreText
    │   ├── ResultCountText
    │   ├── MenuButton_Retry
    │   └── MenuButton_ToTitle
    │
    ├── ToastLayer
    ├── PopupLayer              (ConfirmPopup)
    └── ScreenManager

EventSystem (Input System UI Input Module)
GameRoot
└── GameState
└── OrderGenerator
```

### 9.3 주요 스크립트 역할

| 스크립트 | 역할 |
| :--- | :--- |
| `GameState` | 점수, 남은 시간, 현재 주문, 쟁반, 카운터 보유 |
| `OrderGenerator` | 무작위 주문 생성 (품목 1~3종, 각 1~2개) |
| `ScreenManager` | 화면 활성/비활성 전환, 전환 시 초기 선택 버튼 지정 |
| `HudController` | `GameState` 값을 HUD에 반영 |
| `OrderCardView` | 현재 주문을 `DessertIcon`으로 표시 |
| `TrayView` | 쟁반 슬롯 표시, 담기·취소 처리 |
| `ShelfView` | 진열대 버튼 생성 및 입력 수신 |
| `ToastController` | 알림 메시지 표시·소멸 |

---

## 10. 피드백 설계

| 상황 | 피드백 | 요건 대응 |
| :--- | :--- | :--- |
| 쟁반이 비어 있음 | 제출 버튼 Disabled 색상 + 입력 무시 | 비활성화 표시 |
| 주문 처리 성공 | Success 색 토스트 "주문 완료 +100" | 알림 메시지 |
| 주문 불일치 | Danger 색 토스트 "주문이 다릅니다" | 알림 메시지 |
| 남은 시간 10초 이하 | 게이지가 Danger 색으로 전환 | 색 변화 |
| 버튼 hover / 선택 / 눌림 | Highlighted / Pressed 상태 표현 | 버튼 상태 변화 |
| 쟁반 담김 / 빈칸 | 실선 슬롯 / 점선 슬롯으로 구분 | 상태 표시 |

과제 요건은 2개 이상이며 6개를 배치했습니다.

---

## 11. UI/UX 콘셉트

```text
게임: 제한 시간 안에 손님의 주문을 정확히 처리하는 캐주얼 UI 게임
플레이어가 가장 자주 확인할 것: 현재 주문, 남은 영업시간, 쟁반 상태
UI/UX 콘셉트: "주문은 읽지 않고 알아보게 하고, 실수는 제출 전에 막는다."
적용:
  - 주문 카드와 진열대 버튼에 동일한 아이콘을 사용해
    문자 해독 없이 그림 대조만으로 조작할 수 있게 한다.
  - 쟁반이 비어 있으면 제출 버튼을 비활성화해
    잘못된 제출 자체가 발생하지 않게 한다.
  - 남은 시간이 임계값 아래로 내려가면 게이지 색을 바꿔
    숫자를 읽지 않아도 위험을 알아차리게 한다.
```

이 콘셉트는 "화면을 예쁘게 만든다"가 아니라 **플레이어가 무엇을 빠르게 알아야 하고, 어떤 실수를 사전에 차단할 것인가**를 기준으로 삼습니다. 이후 색상·크기·피드백을 선택할 때 이 문장으로 판단합니다.

---

## 12. 구현 범위 정리

### 12.1 포함

- 단일 씬, 4개 화면 패널 전환
- 손님 1명 단위 주문 처리
- 영업시간 60초 고정
- 품목 4~6종, 주문은 품목 1~3종 × 각 1~2개
- HUD 4종, 프리팹 5종, 피드백 6종
- 마우스 / 터치 / 키보드 입력

### 12.2 제외

| 제외 항목 | 사유 |
| :--- | :--- |
| 난이도 상승 곡선 | 요건 무관, 밸런싱 비용 발생 |
| 콤보·보너스 점수 | 요건 무관 |
| 손님 캐릭터 스프라이트·애니메이션 | 주문 카드로 역할 대체 가능 |
| 최고 점수 저장 | 요건 무관 |
| 효과음 | 요건 무관 |
| 튜토리얼 화면 | 조작이 단순하여 불필요 |
| 대기줄(동시 손님) | 선택 상태 관리가 추가되어 범위 초과 |

범위를 좁게 유지하는 이유는 이 과제의 평가 대상이 게임의 규모가 아니라 **UI/UX 설계·구현·검증 과정**이기 때문입니다.

---

## 13. 작업 일정

| 날짜 | 작업 |
| :--- | :--- |
| 8/18 ~ 8/19 | 설계 문서 작성, 에셋 임포트, 아이콘 선별, TMP 한글 폰트 생성, 9-Slice Border 설정 |
| 8/20 | 프리팹 제작 (DessertIcon, MenuButton, GaugeBar, ToastMessage, ConfirmPopup) |
| 8/21 | 화면 전환 프레임워크 (ScreenManager) 및 4개 화면 골격 배치 |
| 8/22 | `GameState`, `OrderGenerator`, 판정 로직, HUD 바인딩 |
| 8/23 | Input System 연결, Navigation 설정, 키보드 조작 검증 |
| 8/24 | 피드백 구현, 해상도 4종 테스트, **문제 발견 시 즉시 기록** |
| 8/25 | 체크리스트 작성, 7.7절 검증 결과 갱신, 실행 화면 캡처, 문서 최종화 |
| 8/26 | 제출 |

---

## 14. 산출물 대응표

| 제출 산출물 | 대응 |
| :--- | :--- |
| Unity 6 프로젝트 폴더 | 프로젝트 전체 |
| 구현 씬 1개 이상 | `MainScene` |
| 주요 UI 스크립트 | `Assets/Scripts/UI/` |
| 재사용 UI Prefab | `Assets/Prefabs/UI/` |
| UI/UX 설계 문서 | 본 문서 |
| GUI 디자인 가이드 및 구현 가능성 검토 | 본 문서 7장 |
| 자체 체크리스트 | 별도 문서 (8/25 작성) |
| 실행 화면 캡처 2장 이상 | 영업중 / 결과 화면 등 |
